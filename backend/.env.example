# ============================================
# NestJS Backend Environment Variables
# ============================================
# Copy this file to .env and fill in your actual values
# DO NOT commit .env to version control!

# --------------------------------------------
# Backend URL Configuration
# --------------------------------------------
# The full URL where your backend is accessible
# This is used to construct the GitHub OAuth callback URL
# Development: http://localhost:3001 (matches PORT below)
# Production: https://your-backend.onrender.com (your deployed backend URL)
# Note: Do NOT include a trailing slash
BACKEND_URL=http://localhost:3001


# --------------------------------------------
# GitHub OAuth Configuration
# --------------------------------------------
# To get these values:
# 1. Go to https://github.com/settings/developers
# 2. Click "New OAuth App" (or use an existing one)
# 3. Fill in:
#    - Application name: Your app name
#    - Homepage URL: http://localhost:3000 (FRONTEND URL - dev) or https://your-frontend.onrender.com (prod)
#    - Authorization callback URL: {BACKEND_URL}/api/auth/callback
#      Examples:
#        Dev: http://localhost:3001/api/auth/callback
#        Prod: https://your-backend.onrender.com/api/auth/callback
# 4. After creating, you'll get the Client ID and can generate a Client Secret
#
# Note: The callback URL is automatically constructed from BACKEND_URL + '/api/auth/callback'
#       The '/api/auth/callback' path is hardcoded in the backend and cannot be changed.

# GitHub OAuth App Client ID (public identifier)
GITHUB_CLIENT_ID=your_github_client_id_here

# GitHub OAuth App Client Secret (keep this secret!)
GITHUB_CLIENT_SECRET=your_github_client_secret_here

# --------------------------------------------
# JWT Configuration
# --------------------------------------------
# Secret key used to sign JWT tokens for authentication
# Generate a secure random string (at least 32 characters)
# You can generate one using: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=your_super_secret_jwt_key_change_this_in_production

# --------------------------------------------
# Server Configuration
# --------------------------------------------
# Port the BACKEND server will run on
# Used in main.ts: app.listen(process.env.PORT ?? 3000)
# Development: 3001 (backend) - frontend runs on 3000
# Production: Usually 3000 (or set by hosting platform like Render)
# Docker: Set to 3001 in docker-compose
PORT=3001

# Node environment (development, production, test)
# Usually set by Docker (in Dockerfile) or npm scripts
# Uncomment to override:
# NODE_ENV=development
